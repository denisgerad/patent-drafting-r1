What was actually happening
The questions you pasted are correct. Polymer Film Composition, Heating Element Geometry, Film Bonding, Environmental Qualification — all four are right for the heater film patent. The 11% score and the light_guide_plate.json reference were a false result caused by the rater loading the wrong reference file.
The chain of failure was precise:

The RAG pass-1 query ("field of invention... abstract...") retrieved chunks from the ChromaDB collection. That collection may have contained LGP text from a previous session restore, causing extract_field_of_invention() to return the LGP field statement instead of the heater film field statement.
_match_checklist_type(field_of_invention) then matched light_guide_plate on that LGP text, and the rater scored against light_guide_plate.json — giving 11% because heater film questions contain no dot patterns or LGP terms.
The mechanism "Polymer film composition, heating element geometry and distribution" correctly identified the heater film, but _match_checklist_type(mechanism) returned empty because "heating element" was not in the trigger list — only "heater element" was.

Two changes fix this:
product_type_checklists.json — Added "heating element", "flexible heater", and "thin film heater" to the flexible_heater_film trigger list. "heating element" is the standard engineering term; "heater element" was the wrong phrasing.
workflows/scrutiny_workflow.py — Added cross-validation: after matching on field_of_invention, the workflow now also matches on mechanism. If they contradict — LGP field but heater mechanism — the mechanism match wins, because the mechanism is extracted from the task output which used the actual document chunks, making it more reliable than the field string which came from potentially stale RAG results.
When you rerun the heater film patent you should now see flexible_heater_film.json as the reference and a score that reflects how well the questions actually cover heater film topics.Patent rag refactoredZIP DownloadSonnet 4.6